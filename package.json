{
  "name": "pi-claude",
  "version": "1.5.0",
  "private": false,
  "type": "module",
  "description": "Adds a 'Claude Pro/Max Native' subscription provider to Pi's /login that talks to Anthropic exactly like the genuine Claude Code CLI (OAuth, Claude Code identity, beta headers, user-agent, and billing header).",
  "keywords": [
    "pi-package",
    "pi-extension",
    "anthropic",
    "claude",
    "claude-code",
    "oauth",
    "subscription",
    "claude-pro",
    "claude-max"
  ],
  "license": "MIT",
  "files": [
    "src",
    "README.md",
    "VERIFY.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"test/*.test.ts\"",
    "capture:fingerprint": "node --import tsx scripts/capture-fingerprint.mjs",
    "classifier:find": "node --import tsx scripts/bisect-classifier.ts auto"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.80.5",
    "@earendil-works/pi-coding-agent": ">=0.80.5"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.85.0",
    "@earendil-works/pi-coding-agent": "^0.85.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
